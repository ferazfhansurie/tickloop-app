"use client";

import { FinanceSheet } from "./sheet";

/** Standalone full-width view. The same sheet also renders inside the app shell. */
export default function FinancePage() {
  return <FinanceSheet />;
}
